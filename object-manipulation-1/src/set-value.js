/* exported setValue */
// define setValue function with 3 parameters, object, key and value
// nothing to return since it's being passed an object and objects are mutable
// index into object to create the key object[key] = value, and set the value here
function setValue(object, key, value) {
  object[key] = value;
}
// define setValue function with 3 parameters, object, key and value
// nothing to return since it's being passed an object and objects are mutable
// index into object to create the key object[key] = value, and set the value here
