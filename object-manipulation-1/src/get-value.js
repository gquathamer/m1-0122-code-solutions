/* exported getValue */
// define getValue function with 2 parameters, object and key
// direct return the value of the key that is passed to the function like so object[key]
function getValue(object, key) {
  return object[key];
}
// define getValue function with 2 parameters, object and key
// direct return the value of the key that is passed to the function like so object[key]
