/* exported head */
// declare the head function with one parameter, an array
// direct return the first element in whatever array is passed in the function call
function head(array) {
  return array[0];
}
//declare head function with one parameter, an array
// direct return the first element by selection the array parameter at 0 index
  // should work fine even if no array passed or an empty array, return would be undefined
